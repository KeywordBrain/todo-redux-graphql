# todo-redux-graphql

> Can we attach a GraphQL client to redux effectively? Let's find out.

## Development

### Inital Setup
```
npm install
brew install terminal-notifier
```

### Start dev server

`npm start`